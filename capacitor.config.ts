import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'modal-sheet-hans',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
